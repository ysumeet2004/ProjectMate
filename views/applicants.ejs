<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Project Applicants</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: #0a0a0f;
      color: #f5f5f5;
      padding: 40px 20px;
      max-width: 900px;
      margin: auto;
      min-height: 100vh;
    }

    h2 {
      margin-bottom: 40px;
      text-align: center;
      font-weight: 600;
      color: #00e0ff;
      text-shadow: 0 0 12px rgba(0, 224, 255, 0.8);
    }

    .applicant-card {
      background: #12121a;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      padding: 20px 25px;
      box-shadow: 0 0 10px rgba(0, 224, 255, 0.1);
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      transition: all 0.3s ease-in-out;
    }

    .applicant-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 0 20px rgba(0, 224, 255, 0.2);
    }

    .applicant-info h5 {
      font-weight: 600;
      color: #ffffff;
    }

    .applicant-info p {
      color: #bbb;
      margin: 0;
      font-size: 0.95rem;
    }

    .btn-approve {
      background: linear-gradient(90deg, #00e0ff, #00ff88);
      color: #000;
      font-weight: 600;
      border: none;
      padding: 8px 18px;
      border-radius: 6px;
      box-shadow: 0 0 10px rgba(0, 224, 255, 0.5);
      transition: all 0.3s ease;
    }

    .btn-approve:hover {
      box-shadow: 0 0 20px rgba(0, 255, 136, 0.8);
      transform: scale(1.05);
    }

    .btn-success,
    .btn-secondary {
      font-weight: 600;
      border-radius: 6px;
      padding: 8px 18px;
      cursor: default;
    }

    .btn-success {
      background: #00ff88;
      color: #000;
      border: none;
      box-shadow: 0 0 12px rgba(0, 255, 136, 0.7);
    }

    .btn-secondary {
      background: #2a2a38;
      color: #999;
      border: none;
    }

    .no-applicants {
      text-align: center;
      color: #aaa;
      font-size: 1.1rem;
      margin-top: 40px;
    }

    @keyframes glowFade {
      0%, 100% { text-shadow: 0 0 8px #00e0ff, 0 0 16px #00e0ff; }
      50% { text-shadow: 0 0 16px #00ff88, 0 0 32px #00ff88; }
    }

    h2 {
      animation: glowFade 2s infinite alternate;
    }
  </style>
</head>
<body>

  <h2>Applicants for "<%= project_.title %>"</h2>

  <div class="applicant-list">
    <% if (project_.applicants && project_.applicants.length > 0) { %>
      <% project_.applicants.forEach(applicant => { %>
        <div class="applicant-card">
          <div class="applicant-info">
            <h5><%= applicant.user.name || 'Applicant' %></h5>
            <p><%= applicant.user.email || 'No email' %></p>
          </div>

          <div>
            <% if (project_.status === 'FINISHED' || project_.approved_user) { %>
              <% if (project_.approved_user && project_.approved_user.toString() === applicant.user._id.toString()) { %>
                <button class="btn btn-success" disabled>Approved</button>
              <% } else { %>
                <button class="btn btn-secondary" disabled>Not Selected</button>
              <% } %>
            <% } else { %>
              <form action="/project/<%= project_._id %>/applicants/<%= applicant.user._id %>/approve" method="POST">
                <button type="submit" class="btn btn-approve">Approve</button>
              </form>
            <% } %>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p class="no-applicants">There are no applicants for this project yet.</p>
    <% } %>
  </div>

</body>
</html>
